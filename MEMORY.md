# MEMORY.md — project working memory

Read by the agent at the start of every session (see AGENTS.md). Keep it under 200 lines: pointers and decisions, not data.
Format: `## Topic → where to look | one-line gist`.

---

## Overview

"Frazodrom" — an open, non-commercial English trainer, RU→EN by pattern (A1–C1): translation drills, FSRS reviews, listening/speaking, exams, a BYOK AI tutor. Backend-less web PWA, hosted on GitHub Pages, corpus from Tatoeba (CC-BY).

## Pointers

- Plan (normative) → PLAN.md | §0 rules, §16 phases Ф0–Ф9 with acceptance criteria
- Skill map → PLAN.md §3.4 | ~75 skills, stable ids
- Data pipeline → PLAN.md §4 | manythings/Tatoeba → JSON packs, CLI `etr`
- Answer-checking cascade → PLAN.md §7 | normalize → accepted-set → LLM judge → self-check
- AI layer and prompts → PLAN.md §8 | BYOK, role routing, budgets, local models §8.7, model switcher §8.8
- Verified external facts → PLAN.md §17.2 | API limits and versions as of 2026-07-07
- Research queries (user runs these themselves) → PLAN.md §17.3

## Key user decisions

- 2026-07-07: platform — web PWA; public free site; name "Frazodrom"; hosting GitHub Pages; project is open, non-commercial; bilingual UI ru+en; local models (Ollama/LM Studio/llama.cpp) — a bonus option, cloud APIs take priority; user's key — localStorage only, no accounts.
- REWRITE mechanic is mandatory: after the correct answer is shown, the user retypes the translation from a blank field (PLAN.md §6.1).
- The user runs external research themselves using ready-made queries (§17.3) for time-sensitive facts (§17.2: provider pricing/limits/versions). Amended 2026-07-08: agents MAY search the web on their own for ordinary implementation questions (library syntax, API shape, how a tool works) — only §17.2-style facts still route through the user.
- 2026-07-08: AGENTS.md merged into CLAUDE.md (single file, English); user maintains their own AGENTS.md separately for Codex. Added: decision-making-under-uncertainty rules and a standing `implementation-notes.md` convention (see CLAUDE.md).
- 2026-07-08: the project is **international** — no country-specific framing anywhere in docs/UI. Regional unavailability of any LLM provider is handled architecturally: as many adapters as possible (incl. GigaChat, Yandex AI Studio) + routing chain + local models; never region-targeted defaults or wording.

## ACTIVE CONTEXT

- 2026-07-08: PLAN.md v1.3 — 260-question audit applied (~45 fixes) + research results №11–14 folded in (§8.2 provider chain incl. GigaChat/Yandex; §14.4 generated-content license; §4.5 CEFR-SP/ReadMe++ ground truth; §3.4 B1-5 chunk methodology). See session log at the bottom of PLAN.md. Ф3 is split into Ф3а/Ф3б/Ф3в.
- User to confirm agent-drafted defaults in §1.1: positioning paragraph, success criteria, distribution mini-plan.
- 2026-07-08: Ф0 done (app/ + pipeline/ scaffold, CI). Agent decisions/deviations logged in implementation-notes.md (local, gitignored).
- 2026-07-08: user asked the agent to run phases autonomously back-to-back (via ScheduleWakeup self-pacing) instead of the human restarting a session per phase — accepted as an operating-mode override of "one phase = one session"; the phase-by-phase implement→verify→log discipline itself is unchanged. Phases with hard human-only steps (manual proofreading Ф1/Ф6, live user testing Ф8, §17.2/17.3 provider-fact confirmation) still stop and go to BLOCKERS.md.
- 2026-07-08: Ф1 done — full no-LLM pipeline (fetch→clean→tag→level→curate→emit→validate), 11 A1-1/A1-2 skills, `etr validate` 0 problems. CEFR calibration run once against CEFR-SP (HF), thresholds left as-is — flagged for your manual spot-check. 100-sentence review sample is in implementation-notes.md, not a PR.
- 2026-07-08: Ф2 done — checker's 3 outstanding wip-tests fixed (adjacent-letter transposition in editDistance.ts, apologised/apologising, guard cleanup that unblocked short-word typos like "teh"); full Dexie v2 schema (10 tables, §5.3); lazy pack loading from repo-root `packs/` (not duplicated into app/); `DrillEngine` state machine (show→check→REWRITE, hint ladder, requeue) in app/src/engine/; DrillScreen + CourseMapScreen on real pack data; storage protection (persist(), incognito heuristic, Web Locks helper — banner not wired yet); a11y basics (aria-live, color+icon+word, focus-visible, rem). Checker tests 167/167 (AC ≥120 met), app-wide 204/204, Playwright smoke (new, none existed before) measures cold-start→first-sentence at ~1.7s on emulated Fast 3G (local vite preview, not real GH Pages — recheck at Ф8). Not built yet: cross-skill interleave wiring, fluency sprints, contrast duels, SRS field population (all correctly later phases). Judgment calls and full detail in implementation-notes.md.
- Confirmed for user: level-skipping is already in the plan (§11 — placement, module skip-test anytime, level exams), not missing. A1 sentence brevity (2–18 tokens, ≤12 for A1–A2) is an intentional CEFR-level design choice (§4.3), not an accident; corpus's "textbook-ish" feel is a known limitation with mitigations already planned (LLM naturalness pass in Ф6, per-sentence "⚑ bad sentence" flag button, honest README disclaimer) — no plan change needed, just reassurance.
- 2026-07-08: Ф3а mostly done, checkbox NOT ticked (AC not fully met) — see BLOCKERS.md. Built: Gemini + `local-openai` adapters, judge tier 3 (JUDGE_SYSTEM verbatim, zod contract §7.4, retries incl. Cyrillic-check), tier 4 self-check, acceptedCache, per-provider+role daily budgets, model chip with manual override, key validation on save, "Don't wait" abort, `judgeDisputes` table + 🚩 flag, 70-case judge fixture set + benchmark harness (tested against mocked providers only). Fixed one real spec violation found on review: escalation to a final "wrong" was force-revealing the reference/REWRITE, skipping the retry/hint/give-up step §6.1 requires — corrected, 3 new tests lock it in. Vitest 250/250, eslint/tsc/build clean. **Hard blocker (BLOCKERS.md):** §7.5's live benchmark run (60-100 cases against real candidate models, ≥80% accuracy gate, result logged in PLAN.md) needs a user-owned API key — cannot run autonomously. `DEFAULT_ROUTING.judge` is left empty on purpose so nothing becomes a silent unvalidated default judge.
- 2026-07-08: Ф3б done — tutor 4 action buttons (Errors/Explain/Variants/Nuances, verbatim ACTION_* prompts) with `tutorActionCache` (Dexie v4, key includes prompt hash so editing a prompt invalidates old cached replies); ephemeral tutor chat (TUTOR_SYSTEM, 6-turn cap, not persisted); prompt editor in Settings (JUDGE_SYSTEM/TUTOR_SYSTEM/ACTION_*) with reset-to-default and a "default was updated" hash-version note; JUDGE_SYSTEM smoke-test on save (mocked-provider tests only — no live key here). Judgment call: only «Ошибки»/«Разбор» force reveal+REWRITE, not «Варианты»/«Нюансы» — followed §6.1's explicit list over §8.5's looser "any action that reveals the answer" phrasing. Vitest 250→262, eslint/tsc/build clean.
- 2026-07-08: Ф3в done — Groq/OpenRouter adapters (OpenAI-compatible, proxy-by-default), GigaChat (OAuth client-credentials) and Yandex AI Studio adapters (untested against a live account — new BLOCKERS.md entry, possible TLS issue on GigaChat), `proxy/serve.mjs` (dependency-free node:http CORS proxy), auto-switch toast on 429/auth-error via a small event bus (`llm/switchNotifier.ts`), Free Talk MVP (topic picker gated by CEFR, FREETALK_SYSTEM/SUMMARY verbatim, `freeTalkSessions` Dexie v5 table survives tab close with a resume/finish prompt, 15-turn cap, recurring_tags → errorProfile). Vitest 262→299, eslint/tsc/build clean. Judgment call: no user-level tracking exists yet (Ф4), so Free Talk uses a fixed A2 default + an honest hint instead of inventing a leveling system.
- 2026-07-09 — Ф4 partially done, checkbox NOT ticked. Finished a background agent's SRS/FSRS draft (fsrs.ts/leech.ts/errorHunt.ts/dailyPlan.ts/reviewQueue.ts/fluencySprint.ts/contrastDuel.ts/warmup.ts/lessonGen.ts — all reviewed personally, one real tsc bug fixed: `Rating`→`Grade` narrowing for `ts-fsrs`). Built `srs/sessionBookkeeping.ts` (the missing piece tying sessions to skillState's FSRS card + leech checks — nothing wrote to skillState/sessions before this). Generalized `DrillScreen` to run both `/drill/:skillId` (unchanged) and a new `/session` route (multi-skill queues via `engine/sessionLaunch.ts`) with hints/tutor/judge-escalation fully working in both. Rewrote "Сегодня" (`HomeScreen.tsx`) on `buildDailyPlan`: due-skills list, a review button spanning all due skills at once (this is what makes "review mixes ≥2 skills" true live, not just in unit tests), error-hunt button, leech count (informational only), continue-course button. Added §10.6 memory-tier badges to CourseMapScreen. Found+fixed two real pre-existing/newly-exposed bugs via live browser smoke-testing (not caught by vitest): (1) `useI18nStore((s) => ({...}))` object-selector pattern in DrillScreen/SettingsScreen froze the app under React StrictMode ("Maximum update depth exceeded") — shipped since Ф3б, invisible to tests; fixed via separate single-field selectors. (2) `sessionLaunch.ts`'s destructive "take once" pending-session slot broke under StrictMode's double-invoked effects; fixed via non-destructive `peek` + explicit `clear`-on-success. AC (due-logic fake-timers, review ≥2 skills, sprint gate >90%) verified both by tests and live in the browser. **Checkbox intentionally NOT ticked**: fluency-sprint/contrast-duel engine logic is done+tested but has no UI entry point yet (a correct 4/3/2 timer + no-penalty flow doesn't fit the reused REWRITE/escalation engine — didn't want a half-built timer); no 5/15/25-min presets; leech-practice queue not wired (no itemId→skillId index); "skill passed" status never gets set anywhere (blocks contrast-duel's real gating condition). Full detail in implementation-notes.md and PLAN.md's session log.
- 2026-07-12 — **Ф4 fully done, checkbox ticked.** Closed the 4 remaining gaps: (1) `skillState.status` now flips to `'passed'` (sticky) in `sessionBookkeeping.finishSession` once attemptCount≥10 and lifetime accuracy >90% — a documented proxy for real §11 exams, same bar as the fluency-sprint gate. (2) `screens/FluencySprintScreen.tsx` (new, route `/sprint`) — honest 4/3/2 timer, no REWRITE/hints/judge-escalation, own local state (not `DrillEngine`); entry via a "🏃 Спринт" button on CourseMapScreen gated on accuracy>90%. (3) Contrast-duel section on "Сегодня", gated on both paired skills having `status==='passed'`, launches a normal full-`DrillEngine` `/session` (duels teach choosing the form, not tempo — deliberately NOT the no-penalty sprint flow). (4) 5/15/25-min session-length presets on "Сегодня" cap the review/error-hunt queue via `{5:10, 15:30, 25:50}` (agent judgment call — plan only gives the 5-min number). **Found+fixed another real StrictMode double-invoke bug live in the browser**: `FluencySprintScreen` cleared `sessionLaunch`'s pending-session slot synchronously before its first `await`, so React's dev-mode double-invoke raced it away — same bug class as 2026-07-09's `sessionLaunch`/`DrillScreen` fix, just not yet carried into the new screen; fixed the same way (clear only after an `await`, guarded by `cancelled`). Verified live via Playwright driving the system's `/usr/bin/chromium` (no network access to fetch Playwright's own browser build in this sandbox — pointing `executablePath` at the system binary works fine as a fallback). Vitest 357→379, eslint/tsc/build clean.
- 2026-07-12 (later) — **Ф5 built, checkbox NOT ticked** (one AC item needs human ears — BLOCKERS.md). Kokoro-js perf spike done first (§17.3 №3, standalone HTML + Playwright + CDP CPU-throttle proxy for mid-Android, no real device in this environment): RTF>1 even unthrottled (2.5x on a 16-core desktop), ~11-17x under 4x/6x throttle — kokoro-js's WASM backend is well below real-time everywhere, not just on mobile. This drove the whole TTS design: every (text,voice,speed) synthesis is cached as a Blob in a new Dexie `ttsCache` table (v6) so SRS repeats are instant regardless of device speed; background prefetch of the next queue item; Web Speech stays the true-instant default, kokoro is opt-in with an honest loading state. Built `app/src/tts/` (voices/webSpeech/kokoro/cache/settings/speak/kokoroPromptNotifier), `screens/ListeningScreen.tsx` (dictation/comprehension/dictogloss, one screen, no LLM anywhere per §9.2), `engine/listeningQueue.ts`, auto-play wired into `DrillScreen`'s three "correct confirmed" points, TTS panel in Settings, "Аудирование" section on "Сегодня". Exact 28-voice kokoro catalog taken directly from the npm package's file list (§17.2 updated), not guessed. Found+fixed 3 real bugs via testing: (1) build: `SettingsScreen`/`speak.ts` statically importing `tts/kokoro.ts` defeated lazy-loading (Rollup `INEFFECTIVE_DYNAMIC_IMPORT`) — both switched to `await import()`; (2) unit test caught `pickWebSpeechVoice`'s `/male/i` regex matching inside "Female" — UK-male voice was never selectable, fixed to `\bmale\b`; (3) live testing caught `ListeningScreen` reusing `drill.selfCheckPrompt` ("AI key not configured...") for a by-design self-check flow that has nothing to do with LLM availability — added `listening.selfCheckPrompt`. Offline-after-cache genuinely verified: `page.context().setOffline(true)` (true CDP-level, not `page.route`-blocking which also catches dev-server ES-module fetches and gives a false negative) against a **production build** (`npm run preview`), synthesizing a never-before-heard sentence — zero external requests, only an unrelated favicon 404. `kokoro-js` forces `onnxruntime-node` (Node-only, unused by our browser bundle — confirmed by reading both packages' `exports` maps) as a hard dependency with a postinstall that failed in this sandbox; neutralized via `package.json` `overrides: {"onnxruntime-node": "npm:empty-npm-package@1.0.0"}`. Vitest 379→398, eslint/tsc/build clean. **Not verified (BLOCKERS.md, not silently skipped):** AC's "wh-questions sound with question intonation on default voices — check by ear" — agent cannot listen to audio; asked the user to spot-check `af_heart`/`bm_george`/`bm_daniel` via Settings' voice preview.
- 2026-07-12 (later) — user scoped this session to Ф5 only; Ф6/Ф7 explicitly handed off to a separate agent (mcp codex), not attempted here. Next phase to pick up from PLAN.md §16: Ф6 (full A1–B1 course + exams) or Ф7 (speaking), whichever the user/codex starts with — both still unstarted (`[ ]` in §16).
- 2026-07-13 — Ф6/Ф7 remain unticked: added tested pipeline Gemini structured-output cache/parser (`pipeline/src/etr/llm.py`), deterministic placement/exam score rules, and early speaking UI (Chrome STT when available, MediaRecorder playback, read-aloud prompt, YouGlish/Reverso actions), plus a self-hosted-only LanguageTool toggle. Full course packs, golden evaluation, proofread, placement/exam UI and self-reference mode are not built. Environment blocks DNS/Gemini, local browser server and git writes; see BLOCKERS.md. Next: restore those capabilities, then finish Ф6 before treating the partial Ф7 UI as a phase.

## QA contour → see [manual_testing.md](manual_testing.md) | Living manual-test inventory and progress log; checked first MCP drill/settings flow, remaining page actions are tracked by checkbox.
- 2026-07-17: QA work added cross-platform npm scripts, Playwright multi-project configuration, deterministic E2E fixtures, smoke/critical/persistence/AI/layout/accessibility/visual specs, and an explicit manual testing journal. Computer Use is not exposed in the current environment; PWA service worker and live provider/audio checks remain real limitations.
- 2026-07-17: QA verification completed locally: `npm run verify` exit 0 with 406 unit tests and 125 multi-project E2E tests; Python `uv sync`, Ruff, mypy, and 50 pytest passed. Added Free Talk mock E2E coverage and a localized unknown-route recovery screen after MCP found an empty-main defect. Fixed mobile navigation/card overflow, missing Settings labels, WebKit navigation races, and cross-browser mock timing/locator issues. CI now has cross-browser functional, canonical visual/layout, OS smoke, and failure-artifact jobs. Source comments are English; RU remains only as required product localization and learner/test data.
- 2026-07-17: Added `app/src/db/backup.ts` and Settings Data & backup UI. Provider secrets are excluded by default, explicit opt-in is required, import replaces local data, and reset clears every Dexie table. Covered by 5 unit tests, persistence E2E in all six Playwright projects, and MCP download/import/reset smoke. Full app verify is now 411 unit + 131 E2E green; Python remains 50 pytest green.
- 2026-07-17: Added opt-in Gemini 3.1 Flash TTS via the existing BYOK Gemini setting, local audio cache, mocked unit coverage, and no local fallback for automatic drill/listening speech. The system voice remains available only for explicit manual playback; Kokoro remains disabled unless explicitly enabled.
- 2026-07-17: CPU investigation after stopping our Playwright MCP/preview found no remaining Frazodrom Node/browser processes; `HTGame.exe` remained the dominant measured process (~3.46 GB working set), with VS Code processes also active. Full E2E was intentionally stopped during the CPU-priority TTS fix; targeted critical E2E passed 2/2.
- 2026-07-17 (latest QA continuation): MCP layout matrix found configured Settings role-routing cards overflowing at 390×844; added `min-w-0` to routing/readiness grid cards and a deterministic regression in `app/e2e/layout.spec.ts`. Failure reproduced before the fix, then targeted/layout/full checks passed. Latest app verify: 417 unit + 132 multi-project E2E; Python: uv sync, Ruff, mypy, 50 pytest. Final MCP smoke after the fix passed with one persisted attempt, EN reload persistence, local-only network, and zero console errors. Computer Use remains unavailable.
- 2026-07-17 (GitHub + manual continuation): GitHub run #8 confirmed Node 22 fixed the original CI runtime failure; the remaining failure was Ubuntu visual baselines missing because canonical snapshots are `chromium-win32`. Commit `6b099ae` moves visual regression to the Windows OS smoke job; local canonical Chromium visual suite passed 5/5 without snapshot updates. MCP manually covered two-skill review, Error Hunt, Contrast Duel, all listening modes, TTS persistence, mock provider success/fallback/toasts, Tutor rewrite, and Free Talk resume/finish. Controlled mock HTTP failures log browser resource errors but no pageerror; Computer Use remains unavailable.
