# etr — Frazodrom data pipeline

CLI (`etr`) that builds the course packs from the Tatoeba corpus: fetch → clean → tag → level → curate → gapfill → enrich → emit → validate.

Run `uv run etr --help` for the list of commands.
